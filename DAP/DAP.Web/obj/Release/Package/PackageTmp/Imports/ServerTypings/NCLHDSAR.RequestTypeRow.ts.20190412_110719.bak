namespace DAP.NCLHDSAR {
    export interface RequestTypeRow {
        Type?: number;
        Desc?: string;
    }

    export namespace RequestTypeRow {
        export const idProperty = 'Type';
        export const nameProperty = 'Desc';
        export const localTextPrefix = 'NCLHDSAR.RequestType';
        export const lookupKey = 'NCLHDSAR.RequestType';

        export function getLookup(): Q.Lookup<RequestTypeRow> {
            return Q.getLookup<RequestTypeRow>('NCLHDSAR.RequestType');
        }

        export declare const enum Fields {
            Type = "Type",
            Desc = "Desc"
        }
    }
}

